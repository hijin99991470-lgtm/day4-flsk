# Board U/D Feature Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 게시글 상세 페이지에 수정/삭제 기능을 추가하고, 수정은 `templates/write.html` 공용 폼을 재사용하며 삭제 전 확인창을 제공한다.

**Architecture:** 기존 단일 Flask 앱(`app.py`) 구조를 유지한다. 수정은 `GET/POST /posts/<id>/edit` 라우트로 처리하고, 생성/수정 모두 동일한 `write.html` 템플릿을 사용한다. 삭제는 `POST /posts/<id>/delete`로 분리하고 상세 페이지에서 confirm 창을 거친 뒤 서버에서 삭제를 수행한다.

**Tech Stack:** Python 3.10, Flask, sqlite3, pytest, Jinja2, Tailwind CDN

---

### Task 1: Add edit flow with shared `write.html` form

**Files:**
- Create: `templates/write.html`
- Modify: `app.py`
- Modify: `tests/test_board.py`
- (Leave unchanged): `templates/post_form.html`

- [ ] **Step 1: Write the failing tests for edit page and edit submit**

```python
def test_edit_page_prefills_post(client):
    client.post(
        "/posts/new",
        data={"title": "원본 제목", "content": "원본 내용"},
        follow_redirects=True,
    )

    response = client.get("/posts/1/edit")

    assert response.status_code == 200
    body = response.get_data(as_text=True)
    assert "글수정" in body
    assert "value=\"원본 제목\"" in body
    assert "원본 내용" in body


def test_edit_post_updates_content(client):
    client.post(
        "/posts/new",
        data={"title": "수정 전 제목", "content": "수정 전 내용"},
        follow_redirects=True,
    )

    response = client.post(
        "/posts/1/edit",
        data={"title": "수정 후 제목", "content": "수정 후 내용"},
        follow_redirects=True,
    )

    assert response.status_code == 200
    body = response.get_data(as_text=True)
    assert "수정 후 제목" in body
    assert "수정 후 내용" in body
```

- [ ] **Step 2: Run edit tests and verify failure**

Run:
```bash
python -m pytest -q tests/test_board.py::test_edit_page_prefills_post tests/test_board.py::test_edit_post_updates_content
```

Expected:
- FAIL with `404` for `/posts/1/edit` or missing route error

- [ ] **Step 3: Implement minimal edit route and shared form template**

`app.py` change:
```python
@app.route("/posts/new", methods=["GET", "POST"])
def post_create():
    if request.method == "POST":
        title = request.form["title"].strip()
        content = request.form["content"].strip()

        if title and content:
            db = get_db()
            db.execute(
                "INSERT INTO posts (title, content) VALUES (?, ?)",
                (title, content),
            )
            db.commit()

        return redirect(url_for("post_list"))

    return render_template("write.html", mode="create", post=None)


@app.route("/posts/<int:post_id>/edit", methods=["GET", "POST"])
def post_edit(post_id):
    db = get_db()
    post = db.execute(
        "SELECT id, title, content, created_at FROM posts WHERE id = ?",
        (post_id,),
    ).fetchone()

    if post is None:
        abort(404)

    if request.method == "POST":
        title = request.form["title"].strip()
        content = request.form["content"].strip()

        if title and content:
            db.execute(
                "UPDATE posts SET title = ?, content = ? WHERE id = ?",
                (title, content, post_id),
            )
            db.commit()

        return redirect(url_for("post_detail", post_id=post_id))

    return render_template("write.html", mode="edit", post=post)
```

Create `templates/write.html`:
```html
<!doctype html>
<html lang="ko">
  <head>
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{% if mode == 'edit' %}글수정{% else %}글쓰기{% endif %}</title>
    <script src="https://cdn.tailwindcss.com"></script>
  </head>
  <body class="bg-[#FAF9F6] text-stone-800">
    <main class="max-w-2xl mx-auto px-5 py-10">
      <header class="mb-8">
        <h1 class="text-3xl font-semibold tracking-tight">{% if mode == 'edit' %}글수정{% else %}글쓰기{% endif %}</h1>
      </header>

      <form method="post" class="space-y-4 bg-white border border-stone-200 rounded-xl p-5">
        <div>
          <label for="title" class="block text-sm font-medium mb-2">제목</label>
          <input id="title" name="title" required value="{{ post['title'] if post else '' }}" class="w-full rounded-lg border-stone-300" />
        </div>

        <div>
          <label for="content" class="block text-sm font-medium mb-2">내용</label>
          <textarea id="content" name="content" required rows="10" class="w-full rounded-lg border-stone-300">{{ post['content'] if post else '' }}</textarea>
        </div>

        <div class="flex gap-3">
          <button type="submit" class="px-4 py-2 rounded-xl bg-amber-700 text-white">{% if mode == 'edit' %}수정{% else %}저장{% endif %}</button>
          <a href="{% if post %}{{ url_for('post_detail', post_id=post['id']) }}{% else %}{{ url_for('post_list') }}{% endif %}" class="px-4 py-2 rounded-xl border border-stone-300">취소</a>
        </div>
      </form>
    </main>
  </body>
</html>
```

- [ ] **Step 4: Run edit tests and verify pass**

Run:
```bash
python -m pytest -q tests/test_board.py::test_edit_page_prefills_post tests/test_board.py::test_edit_post_updates_content
```

Expected:
- PASS (2 passed)

- [ ] **Step 5: Commit task 1**

```bash
git add app.py templates/write.html tests/test_board.py
git commit -m "feat: add post edit route with shared write form"
```

### Task 2: Add delete route and detail page edit/delete controls

**Files:**
- Modify: `app.py`
- Modify: `templates/post_detail.html`
- Modify: `tests/test_board.py`

- [ ] **Step 1: Write the failing tests for delete and detail controls**

```python
def test_detail_page_shows_edit_delete_buttons_and_confirm(client):
    client.post(
        "/posts/new",
        data={"title": "버튼 확인", "content": "내용"},
        follow_redirects=True,
    )

    response = client.get("/posts/1")

    assert response.status_code == 200
    body = response.get_data(as_text=True)
    assert "/posts/1/edit" in body
    assert "/posts/1/delete" in body
    assert "정말 삭제할까요?" in body


def test_delete_post_removes_item(client):
    client.post(
        "/posts/new",
        data={"title": "삭제 대상", "content": "삭제 내용"},
        follow_redirects=True,
    )

    response = client.post("/posts/1/delete", follow_redirects=True)

    assert response.status_code == 200
    body = response.get_data(as_text=True)
    assert "삭제 대상" not in body
```

- [ ] **Step 2: Run delete/control tests and verify failure**

Run:
```bash
python -m pytest -q tests/test_board.py::test_detail_page_shows_edit_delete_buttons_and_confirm tests/test_board.py::test_delete_post_removes_item
```

Expected:
- FAIL with missing `/posts/<id>/delete` route and missing button/confirm markup

- [ ] **Step 3: Implement minimal delete route and detail buttons**

`app.py` add:
```python
@app.route("/posts/<int:post_id>/delete", methods=["POST"])
def post_delete(post_id):
    db = get_db()
    db.execute("DELETE FROM posts WHERE id = ?", (post_id,))
    db.commit()
    return redirect(url_for("post_list"))
```

`templates/post_detail.html` replace `<article ...>` block with controls included:
```html
<article class="mt-4 bg-white rounded-xl border border-stone-200 p-6">
  <h1 class="text-3xl font-semibold tracking-tight mb-3">{{ post['title'] }}</h1>
  <p class="text-sm text-stone-400 mb-6">{{ post['created_at'] }}</p>
  <div class="text-stone-700 whitespace-pre-wrap leading-7">{{ post['content'] }}</div>

  <div class="mt-6 flex gap-3">
    <a href="{{ url_for('post_edit', post_id=post['id']) }}" class="px-4 py-2 rounded-xl border border-stone-300">수정</a>
    <form method="post" action="{{ url_for('post_delete', post_id=post['id']) }}" onsubmit="return confirm('정말 삭제할까요?')">
      <button type="submit" class="px-4 py-2 rounded-xl bg-red-600 text-white">삭제</button>
    </form>
  </div>
</article>
```

- [ ] **Step 4: Run delete/control tests and verify pass**

Run:
```bash
python -m pytest -q tests/test_board.py::test_detail_page_shows_edit_delete_buttons_and_confirm tests/test_board.py::test_delete_post_removes_item
```

Expected:
- PASS (2 passed)

- [ ] **Step 5: Commit task 2**

```bash
git add app.py templates/post_detail.html tests/test_board.py
git commit -m "feat: add post delete route and detail page controls"
```

### Task 3: Run full regression and finalize

**Files:**
- Modify: none (verification only)
- Test: `tests/test_board.py`

- [ ] **Step 1: Run full test suite**

Run:
```bash
python -m pytest -q
```

Expected:
- PASS (all tests passed)

- [ ] **Step 2: Manual UI smoke check**

Run:
```bash
python app.py
```

Verify in browser:
- `/posts/new`에서 게시글 생성 가능
- 상세 페이지에 `수정`, `삭제` 버튼 노출
- 수정 페이지에서 기존 제목/내용 프리필
- 삭제 버튼 클릭 시 `정말 삭제할까요?` confirm 노출
- confirm 수락 시 목록으로 이동하고 게시글 사라짐

- [ ] **Step 3: Final commit for any verification-driven fix (if needed)**

```bash
git status --short
```

Expected:
- No changes; if changes exist from bugfixes, commit with:
```bash
git add <changed-files>
git commit -m "fix: resolve remaining issues after U/D verification"
```
